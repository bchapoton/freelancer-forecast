import { styled } from '@mui/system';
import { TableCell, TableRow } from '@mui/material';

export const TableCellBold = styled(TableCell)({
    fontWeight: 'bold',
});

export const TableRowFooter = styled(TableRow)({
    backgroundColor: '#f8f9fa',
});
